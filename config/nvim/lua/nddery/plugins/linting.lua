-- Linting is handled by the language servers themselves (the `eslint` LSP
-- provides JS/TS diagnostics + code-actions). nvim-lint is intentionally not
-- installed. Add it back here only for linters that have no LSP, e.g.
-- markdownlint.
return {}
